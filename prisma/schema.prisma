generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
}

model WaitingList {
  id        String   @id @default(cuid())
  email     String   @unique
  ip        String?
  createdAt DateTime @default(now())

  @@index([email])
}
